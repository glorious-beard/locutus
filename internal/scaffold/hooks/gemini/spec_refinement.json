{
  "_comment": "Locutus hooks for Gemini on the spec_refinement activity. Generated by the Locutus publisher (DJ-136 phase 6); re-emitted on every `locutus update --reset`. Inserted into .gemini/settings.json's hooks field. BeforeTool on mcp__locutus__spec_propose_decision enforces the DJ-133 id-shape invariant via the locutus hook-validate-decision subcommand.",
  "hooks": [
    {
      "event": "BeforeTool",
      "matcher": "mcp__locutus__spec_propose_decision",
      "command": "{{LOCUTUS_BIN}}",
      "args": ["hook-validate-decision"]
    }
  ]
}
